# Changelog

## 0.2.2

> 野旷天低树，江清月近人。

枝理 Tab 0.2.2 让分组整理更直接，也让展开后的空间更从容：休眠与关闭操作从菜单中来到卡片表面，一次点击即可完成；分组详情则根据内容与窗口尺寸自然伸缩，在标签较多时保持标题操作区稳定，并把滚动留给标签列表。

### 优化

- 将“休眠本组”和“关闭本组”设为分组卡片上的常驻快捷按钮，减少批量整理的操作层级。
- 为批量操作补充按压、禁用和执行中反馈，并优化标签及分组关闭时的离场动画。
- 调整分组详情的动态高度策略：少量标签按内容自然展开，大量标签在可用空间内滚动。
- 保持展开标题与批量操作区可见，避免详情面板覆盖底部诗文区域。

## 0.2.1

> 潮平两岸阔，风正一帆悬。

枝理 Tab 0.2.1 让新标签页更自然地适应不同尺寸的窗口：从紧凑小屏到宽阔桌面，分组面板、标签列表与诗文区域会随可用空间流动调整，在保留信息层次的同时减少拥挤、遮挡与不必要的留白。

### 优化

- 新增多分辨率流式布局，让主面板的宽度、高度与间距随视口平滑调整。
- 优化窄屏和低高度窗口中的分组网格与标签列表，提升有限空间下的可读性和可操作性。
- 优化诗文区域的响应式排版与动效定位，让展开、切换和悬浮操作在不同尺寸下保持稳定。
- 统一 CSS 长度解析和动效位移计算，减少布局变化时的跳动。

## 0.1.9

> 行到水穷处，坐看云起时。

枝理 Tab 0.1.9 把诗文体验带回本地：底部诗文不再依赖外部接口，而是从内置精选诗库中按日期生成每日固定诗单。每一天，所有人都会看到同一组诗；想换一首时，也只是在当天诗单里安静流转。同时，本版优化了站点图标获取和无界书签栏细节，让新标签页更稳、更干净。

### 新增

- 新增本地精选诗库，诗文展示不再依赖第三方诗词接口。
- 新增每日固定诗单，每天从精选诗库中生成 10 首诗文。
- 新增诗文“换一首”按钮，可在当天诗单中循环切换。
- 新增 Chrome/Edge favicon 回退能力，提升标签和书签图标显示稳定性。

### 优化

- 优化诗文筛选策略，优先展示更熟知、更工整、更适合新标签页展示的诗文。
- 优化诗文数据为统一简体，并过滤过长或展示效果不佳的内容。
- 优化无界书签栏菜单布局、行高和图标显示。
- 更新隐私说明，明确诗文展示使用本地数据。

## 0.1.8

> 春风得意马蹄疾，一日看尽长安花。

枝理 Tab 0.1.8 聚焦书签栏体验：新增无界书签栏样式，并补齐标准与无界两种模式下的多级目录展开、滚动高度、菜单间距和轻动画。书签栏内部也拆分为更清晰的组件与 hooks，后续继续调整视觉细节时会更稳、更顺手。

### 新增

- 新增无界书签栏样式，可在设置中从标准样式切换。
- 新增横向根书签与多列级联菜单，支持更深层级的书签目录浏览。

### 优化

- 优化标准与无界书签栏的共用渲染结构，减少两套菜单行为分叉。
- 优化无界菜单的滚动高度、目录项间距、图标文字间隔和 hover 反馈。
- 优化多级目录展开逻辑，避免鼠标经过时误触发更深层菜单。
- 拆分书签栏组件、状态 hooks、菜单基础组件和纯工具函数，降低后续维护成本。

## 0.1.7

> 如切如磋，如琢如磨。

枝理 Tab 0.1.7 继续打磨日常使用里的细节：设置面板更清爽紧凑，搜索模式切换与外观控制的反馈更顺滑；诗文展示新增作者朝代显示开关；底层波浪背景减少后台与高分屏场景的内存占用，让新标签页在长时间打开时更稳。

### 新增

- 新增诗文作者朝代显示设置，可选择显示“朝代 · 作者”或仅显示作者名。
- 新增 GitHub 项目入口，可在关于区域快速查看源码、版本更新和项目动态。

### 优化

- 优化设置面板、分段控制、颜色选择、搜索模式切换预览等控件的视觉层级与交互反馈。
- 优化诗文操作按钮组和展开态间距，让复制、搜索和返回操作更统一。
- 优化波浪背景渲染策略，降低画布像素预算，并在页面隐藏时释放画布内存。
- 补充项目截图与商店安装入口，完善发布标签题记格式说明。

## 0.1.6

> 何时一樽酒，重与细论文。

枝理 Tab 0.1.6 聚焦于分组卡片的空间展开体验：点击分组后，当前卡片会从原位置连续扩张为详情面板，内部标签从紧凑列表切换为完整条目，用户可以通过返回区域或 ESC 收起，让整理标签时的焦点切换更安静、更连贯。

### 新增

- 新增分组卡片 Shared Layout Focus Expand 交互，支持从概览网格连续展开到详情面板。
- 新增分组详情返回交互，可点击展开后的 header 区域或按 ESC 收起。

### 优化

- 优化分组展开时的布局承载方式，详情面板覆盖完整分组列表区域，避免非首个分组展开时出现顶部留白。
- 复用现有标签条目组件，在概览中保持紧凑模式，在详情中使用完整模式，不改变既有 UI 风格。

## 0.1.5

> 可怜楼上月徘徊，应照离人妆镜台。

枝理 Tab 0.1.5 是一次轻量补丁更新，主要补足了诗文区域的细节交互，并整理了应用内部状态结构，让新标签页在后续继续迭代时更稳、更容易维护。

### 新增

- 新增诗文单句复制操作，鼠标悬停在诗句行上即可快速复制当前这一句。

### 优化

- 优化诗文标题区域的悬浮操作呈现，复制全文、搜索诗文等入口更顺手。
- 优化部分搜索栏与设置面板的交互细节，让操作反馈更清晰自然。

### 稳定性

- 重构应用状态管理与标签结果渲染结构，减少主界面耦合，为后续功能迭代和稳定性修复打底。

## 0.1.4

> 江畔何人初见月，江月何年初照人？

枝理 Tab 0.1.4 聚焦于让新标签页更轻、更顺：诗文展开动画更柔和，悬浮操作可复制诗文或用浏览器默认搜索查询；搜索框聚焦态更清晰；应用结构拆分为更小的组件与 hooks，后续维护和迭代更稳定。同时移除自定义搜索引擎配置，改用浏览器默认搜索服务，并补充隐私说明。

### 新增

- 新增诗文复制与诗文搜索入口，可在诗文悬浮操作中快速使用。
- 新增隐私政策文档，说明权限、数据存储与第三方诗词服务使用方式。

### 优化

- 优化诗文展开、收起与浮起动画，让底部诗文呈现更自然。
- 优化搜索框聚焦态，提升键盘输入和视觉反馈的清晰度。
- 将主界面拆分为 Header、控制栏、标签区域、色彩菜单等组件，并抽取主题色相关 hooks。
- 精简样式表与点缀字体配置，移除冗余 CSS 规则。

### 调整

- 移除自定义搜索引擎选择，网络搜索统一使用浏览器默认搜索提供方。

## 0.1.3

> 庄生晓梦迷蝴蝶，望帝春心托杜鹃。

枝理 Tab 0.1.3 带来更安静也更实用的新标签页体验：新增底部诗文展示，可从今日诗词获取每日诗句，并支持点击展开完整内容；搜索栏升级为标签搜索与网络搜索双模式，可快速切换并选择 Google、DuckDuckGo、Bing 或 Baidu；同时优化了域名分组、特殊浏览器页面识别、设置面板和暗色主题细节，让标签整理、查找和浏览入口更加顺手。

### 新增

- 新增底部诗文展示，接入今日诗词 API，并支持点击展开诗文全文。
- 新增设置项，可控制是否展示底部诗文。
- 新增网络搜索模式，搜索框可在标签搜索和网络搜索之间切换。
- 新增搜索引擎选择，支持 Google、DuckDuckGo、Bing、Baidu。
- 新增搜索图标样式设置，可选择通用图标或当前搜索引擎图标。

### 优化

- 优化域名分组，支持识别 `google.com.hk`、`google.co.uk` 等多段公共后缀。
- 优化 `chrome://`、`edge://`、`about:blank`、扩展页等特殊页面的分组与展示。
- 优化设置面板、搜索模式切换、底部诗文、暗色主题和点缀字体等视觉细节。
- 集中整理基础字体栈与点缀字体变量，便于后续发布前检查字体授权、CSP、隐私和离线表现。

### 权限

- 新增 `https://v2.jinrishici.com/*` 站点权限，用于获取诗词内容。
